# VPN (OVPN and IPsec)

OpenVPN server and IPsec peer/proposal management. WireGuard is covered separately.

See `vpn.py` for tools on `/interface ovpn-server`, `/ip ipsec peer`, and `/ip ipsec proposal`.
