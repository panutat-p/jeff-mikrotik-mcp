# Containers

Manage RouterOS containers, mounts, environment variables, and config. Requires container package and device-mode.

See `containers.py` for tools: `list_containers`, `create_container`, `start_container`, `stop_container`, mount/env/config CRUD.
